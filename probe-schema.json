{
  "//": [
    "Shape contract for the document ngx_test_probe_json() renders.",
    "",
    "This file exists because field drift is only half-guarded. A rule that",
    "NAMES a field already fails loudly when the probe stops emitting it",
    "(assert.c: 'probe path \"...\" not present in document'). What nothing",
    "catches is the other half: a field that is renamed, retyped or dropped",
    "while NO current rule references it. That stays invisible until someone",
    "writes a rule against it months later and gets a failure that reads like",
    "a bug in their rule rather than in the probe.",
    "",
    "So this is a schema for the EMITTER, not a validator for untrusted input.",
    "The probe document is produced by our own code; the risk being managed is",
    "our own drift.",
    "",
    "'required' is per the variant a document belongs to. zone.present is the",
    "discriminator: when the probe has no zone (or the zone's shm is not mapped",
    "yet) the object carries present:false and nothing else is promised.",
    "",
    "Module hooks may add members INSIDE the zone object via zone_render, and",
    "own the entire contents of the top-level 'module' object via",
    "module_render. Those are deliberately not listed -- they belong to the",
    "consuming module, not to this contract -- which is why unknown members",
    "under 'zone' and 'module' are allowed while unknown members at the top",
    "level are not."
  ],

  "version": 1,

  "fields": {
    "flavor":                 { "type": "string", "required": true },
    "flavor_version":         { "type": "string", "required": true },
    "pid":                    { "type": "number", "required": true },
    "ppid":                   { "type": "number", "required": true },
    "config_generation":      { "type": "number", "required": true },
    "page_size":              { "type": "number", "required": true },
    "connections":            { "type": "object", "required": true },
    "connections.total":      { "type": "number", "required": true },
    "connections.free":       { "type": "number", "required": true },
    "fds":                    { "type": "number", "required": true },
    "timers":                 { "type": "number", "required": true },
    "fds_by_kind":            { "type": "object", "required": true },
    "fds_by_kind.socket":     { "type": "number", "required": true },
    "fds_by_kind.file":       { "type": "number", "required": true },
    "fds_by_kind.anon":       { "type": "number", "required": true },
    "fds_by_kind.other":      { "type": "number", "required": true },
    "smaps":                  { "type": "object", "required": true },
    "smaps.pss":              { "type": "number", "required": true },
    "smaps.private_dirty":    { "type": "number", "required": true },
    "pool":                   { "type": "object", "required": true },
    "pool.cycle_used":        { "type": "number", "required": true },
    "pool.cycle_blocks":      { "type": "number", "required": true },
    "pool.cycle_large":       { "type": "number", "required": true },
    "pool.cycle_cleanup":     { "type": "number", "required": true },
    "redzone":                { "type": "object", "required": true },
    "redzone.violations":     { "type": "number", "required": true },
    "redzone.checked":        { "type": "number", "required": true },
    "canary":                 { "type": "object", "required": true },
    "canary.violations":      { "type": "number", "required": true },
    "canary.checked":         { "type": "number", "required": true },
    "canary.live":            { "type": "number", "required": true },
    "module":                 { "type": "object", "required": false },
    "zone":                   { "type": "object", "required": true },
    "zone.present":           { "type": "bool",   "required": true },

    "zone.name":             { "type": "string", "required": "when-zone-present" },
    "zone.size":             { "type": "number", "required": "when-zone-present" },
    "zone.slab_pages_free":  { "type": "number", "required": "when-zone-present" },
    "zone.slab_reqs":        { "type": "number", "required": "when-zone-present" },
    "zone.slab_fails":       { "type": "number", "required": "when-zone-present" },
    "zone.slab_used":        { "type": "number", "required": "when-zone-present" },
    "zone.digest":           { "type": "number", "required": "when-zone-present" }
  },

  "closed": ["", "connections", "fds_by_kind", "smaps", "pool"],

  "notes": {
    "fds": "-1 means /proc was unreadable, NOT zero descriptors. A delta over that sentinel cancels to a passing zero, which is why the prober rejects it explicitly rather than subtracting it.",
    "fds_by_kind": "The same descriptor total split by the kind of each /proc/self/fd link (socket/file/anon/other). Every bucket is -1 together if the scan cannot complete; the prober rejects that sentinel like it does for fds. socket:/anon_inode: links are matched by prefix, a leading '/' is a file, everything else (pipe:[...]) is other.",
    "zone.slab_reqs": "Cumulative ngx_slab_alloc_locked() calls against this zone, summed over every size slot of pool->stats[]. Never decreases, so unlike slab_pages_free (occupancy) it exposes allocation TRAFFIC: ten thousand matched alloc/free pairs per request net to zero in occupancy and read as clean, while reqs shows the churn. The figure a `delta zone.slab_reqs <= K` ceiling asserts on; K is a property of the module under test, not of this harness, so no default is offered. -1 means the zone's slab pool is mapped but not yet initialised (stats[] still NULL); the prober rejects that sentinel like it does for fds.",
    "zone.slab_fails": "Cumulative allocation FAILURES against this zone (pool->stats[].fails), summed over slots. Nonzero means the slab was exhausted at least once -- the condition fault_slab= arms artificially. -1 is the same uninitialised-pool sentinel as slab_reqs.",
    "zone.slab_used": "LIVE allocated blocks in this zone (pool->stats[].used), summed over slots: incremented on alloc, decremented on free. This one is occupancy, so it is the leak oracle of the three, while slab_reqs is the churn oracle. -1 is the same uninitialised-pool sentinel as slab_reqs.",
    "smaps": "PSS and Private_Dirty in kB from /proc/self/smaps_rollup. External-observer memory lineage the cycle-pool walk cannot give -- growth in ANY mapping. Both -1 where smaps_rollup is absent (pre-4.14 kernel or a sandbox); the prober rejects that sentinel like fds.",
    "pool.cycle_cleanup": "Length of the cycle pool's cleanup-handler chain. Names a cleanup parked on the cycle pool per request -- a leak the byte count also reflects but this identifies by shape.",
    "zone.digest": "An order-independent mix of this zone's own accounting (slab_reqs, slab_fails, slab_used), for CROSS-WORKER comparison. The zone is the same bytes in every worker, so two workers reading it AT REST must produce the same digest; a disagreement is itself a finding. NOT a cryptographic hash and never to be described as one -- it is a change detector, and a collision means two different zone states hash alike and a divergence goes unreported. Deliberately excludes slab_pages_free, which legitimately differs between two reads taken microseconds apart while a request is in flight; including it would make the digest disagree between workers under perfectly healthy load. -1 is the same uninitialised-pool sentinel as slab_reqs.",
    "canary.violations": "Slab guard-byte corruptions this WORKER has detected. A shm zone is one mmap created before the fork, so ASan and memcheck see a single region with no per-chunk boundaries to poison -- and the corruption may have been written by a DIFFERENT worker, which no single-address-space tool models at all. Counted by the process that LOOKED, never attributable to the one that broke it. Also counts a mismatched alloc/free pair (a pointer this probe did not hand out) and a double free, both caught by the in-band header magic.",
    "canary.checked": "Slab chunks this worker has verified. Exists to make canary.violations == 0 falsifiable: zero violations out of zero checked chunks is the vacuous pass a consumer gets by wiring the probe in and never routing an allocation through ngx_test_probe_slab_alloc(). Assert BOTH.",
    "canary.live": "Guarded chunks handed out and not yet returned. At rest this is a leak oracle for the module's own bookkeeping that zone.slab_used cannot express: slab_used counts what the ALLOCATOR still holds and is decremented by anyone freeing the chunk, while live counts what this probe handed out and never got back.",
    "module": "Rendered only when the module under test registers a module_render hook, and independently of any shm zone -- it is the only path into the document for a module that has no zone at all (a compression body filter keeps its state in the request pool and per-worker globals). Absent entirely otherwise, never an empty stub. Its CONTENTS belong to the consuming module, so it is an open level like zone; the top level stays closed, which is why the object itself has to be named here.",
    "zone.present": "false has two causes -- no zone configured at all, and a zone whose shm.addr is not mapped yet (a probe racing a reload). Neither promises the sibling members."
  }
}
